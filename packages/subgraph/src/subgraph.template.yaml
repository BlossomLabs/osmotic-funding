specVersion: 0.0.2
schema:
  file: ./src/schema.graphql
dataSources:
  - kind: ethereum/contract
    name: SuperConvictionVoting
    network: localhost
    source:
      address: "{{localhost_SuperConvictionVotingAddress}}"
      abi: SuperConvictionVoting
      startBlock: 1
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - Purpose
        - Sender
      abis:
        - name: SuperConvictionVoting
          file: ./abis/localhost_SuperConvictionVoting.json
      eventHandlers:
        - event: SetPurpose(address,string)
          handler: handleSetPurpose
      file: ./src/mapping.ts
